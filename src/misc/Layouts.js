import tw from "twin.macro";

export const HeaderWrapper = tw.div`relative max-w-screen-xl mx-auto min-h-full p-8 overflow-hidden`;
export const HeaderContainer = tw.div`relative -mx-8 -mt-8`;
export const Wrapper = tw.div`relative`;
export const Container = tw.div`max-w-screen-xl mx-auto py-20`;
